---
import { stats } from '../../utils/url';

const res = await fetch(stats.url.contributions);

return new Response(res.body, {
    headers: {
        'Content-Type': res.headers.get('Content-Type') || '',
    },
});
---
